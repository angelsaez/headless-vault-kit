# Fences

A real link: [[Note]]

Inline code, must not count as a link: `[[Note]]`

```markdown
[[Note]] inside a fenced code block, must not count.
#not-a-tag
- [ ] not a task either
```

    [[Note]] inside an indented code block, must not count.

%%
[[Note]] inside an Obsidian comment. Behaviour to confirm against the app.
%%
